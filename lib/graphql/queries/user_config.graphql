query UserConfig {
    userLoadConfig {
        authorized
        refreshToken
        userId
        meta {
            id
            alias
            avatarUrl
            teaserUrl
            lastLogin
            email
            role
            superUser
            userType
            settings {
                public
                searchable
                showFullName
                showEmail
            }
        }
    }
}